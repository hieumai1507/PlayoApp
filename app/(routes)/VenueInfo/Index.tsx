import { View, Text } from "react-native";
import React from "react";
import VenueInfoScreen from "@/screen/VenueInfoScreen/VenueInfo.Screen";

const VenueInfo = () => {
  return <VenueInfoScreen />;
};

export default VenueInfo;
