import { StyleSheet, Text, View } from "react-native";
import React from "react";
import PlayScreen from "@/screen/PlayScreen/Play.Screen";

const Play = () => {
  return <PlayScreen />;
};

export default Play;

const styles = StyleSheet.create({});
