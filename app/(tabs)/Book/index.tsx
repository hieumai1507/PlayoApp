import { View, Text } from "react-native";
import React from "react";
import BookScreen from "@/screen/BookScreen/Book.Screen";

const Book = () => {
  return <BookScreen />;
};

export default Book;
